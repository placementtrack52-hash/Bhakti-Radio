export interface Deity {
  id: number;
  name: string;
  slug: string;
  image: string;
  icon: string;
  description: string;
}

export interface Category {
  name: string;
  slug: string;
  icon: string;
}
